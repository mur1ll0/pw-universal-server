#pragma once
// `EC_RoleTypes.h` inclui <ABaseDef.h>, um cabeçalho do motor gráfico que não descreve
// formato de fio nenhum. Vazio de propósito: o que importa em EC_RoleTypes.h são as
// structs ROLEEXTPROP*, que não dependem de nada dele.
