[package]
name = "pw-protocol"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true

[dependencies]
pw-core = { path = "../pw-core" }
pw-crypto = { path = "../pw-crypto" }
bytes = { workspace = true }
byteorder = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
tracing = { workspace = true }
tokio-util = { workspace = true }
hex = { workspace = true }

[dev-dependencies]
# Enquanto `octets.rs` não migra, o teste cruzado prova que as duas implementações
# do formato GNET produzem os mesmos bytes.
pw-wire = { path = "../pw-wire" }
# Só nos testes que conferem os opcodes contra os IRs de `specs/protocol/`.
serde_json = { workspace = true }
